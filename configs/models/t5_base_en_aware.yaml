model_name: t5-base
language: en
mode: answer_aware
tokenizer_name: t5-base

base_config: ../base_config.yaml

dataset:
  name: squad_v2
  language: en
  unanswerable_ratio: 0.33

training:
  num_epochs: 5
  output_dir: ./checkpoints/t5_base_en_aware

lora:
  enabled: true
  r: 32
  lora_alpha: 64
  lora_dropout: 0.1
